- Create `mkosi.extra/usr/lib/repart.sysinstall.d`, add `CopyBlock=` to `usr.conf` and add `CopyFiles=` to `esp.conf` when `systemd-sysinstall` is added to systemd.
- ~~Disable `kernel-install` scripts when `systemd-sysinstall` is added to systemd (use `CopyFiles=` in `esp.conf` instead)~~(This is abandoned).
- Stop making symlinks under `/var/lib/confexts` when `/var/lib/extensions` can save both sysexts and confexts (systemd TODO).
- When `start-extension-service.service` is deleted, `systemd-networkd-wait-online.service -> /dev/null` symlink in confext will not work (as expected to mask `s-n-w-o.service`) until systemd #39410 is merged.
	- Drop `start-extension-services.service` when systemd #39410 is merged.
- `mkosi.images/.dep-desktop/mkosi.extra/etc/environment.d/99-glib-schema.conf` will not work (as expected to mask set environment) until systemd #39410 is merged.
	- We run `systemctl set-environment` manually in `start-extension-services.service`.
- Add drop-in config for `systemd-confext-sysroot.service` (`exec.conf`) in default initrd when systemd #39410 is merged.
	- Then use `systemd-tmpfiles` to manage `/etc/shells` (clean, then write).
- Move `update-etc.service` (maybe need to add `RootDirectory=/sysroot`) to initrd and run before `systemd-confext-sysroot.service` when systemd #39410 is merged (but no need for `update-package-metadata.service`).
- Drop `homed2accounts` when [accountsservice 25.34.76](https://gitlab.freedesktop.org/accountsservice/accountsservice/-/releases/25.34.76) is packaged by Debian.
- Write JSON user records for sysexts.
- Write a script to automatically install `mkosi`, clone the repo, and build `ext-desktop`.
- Maybe use new sysinstall initrd style (base UKI with extension) when sysinstall is added to systemd (#38764), so that we can split some firmware into ext image.
