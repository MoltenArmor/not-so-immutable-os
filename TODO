- Create `mkosi.extra/usr/lib/repart.sysinstall.d`, add `CopyBlock=` to `usr.conf` and add `CopyFiles=` to `esp.conf` once `systemd-sysinstall` is added to systemd.
- Disable `kernel-install` scripts once `systemd-sysinstall` is added to systemd (use `CopyFiles=` in `esp.conf` instead).
