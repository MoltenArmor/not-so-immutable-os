#!/bin/sh
set -ue

TMP="$(mktemp -d)"

clean() {
    rm -rf "${TMP}"
}
trap EXIT clean

curl -sSL "https://github.com/helix-editor/helix/releases/download/25.07/helix_25.7.0-1_amd64.deb" -o "${TMP}/helix.deb"

mkosi-install "${TMP}/helix.deb"
