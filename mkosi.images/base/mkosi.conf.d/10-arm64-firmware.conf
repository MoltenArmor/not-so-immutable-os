[Match]
Architecture=arm64
Distribution=debian

[Content]
Packages=?name(^firmware-.*$)
    !?name(^.*installer$)
    !?name(^firmware-nvidia-gsp-.*$)
    !?name(^firmware-nvidia-tesla.*$)
    !?exact-name(firmware-crystalhd)
    !?exact-name(firmware-realtek-rtl8723cs-bt)
    !?exact-name(ezurio-qca-firmware)
    !?exact-name(expeyes-firmware-dev)
    !?exact-name(linux-firmware-raspi3)
    !?exact-name(raspi3-firmware)
    bluez-firmware
