#!/bin/sh -ue

FAKE_KERNEL="$(ls -1 /usr/lib/modules/ | head -n 1)"
export FAKE_KERNEL
PATH="/usr/local/bin:${PATH}"

cd /root && \
tar -xvf vtoyboot.tar.gz

cd /root/vtoyboot && bash ./vtoyboot.sh
dracut -v -f /boot/initrd.img "${FAKE_KERNEL}"

mv /boot/initrd.img "${OUTPUTDIR}/"

